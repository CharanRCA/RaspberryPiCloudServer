# Install as /etc/ssh/sshd_config.d/00-cloud-storage.conf.
# The 00 prefix must precede cloud-init because OpenSSH uses the first value set.
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
AllowUsers cloud

